# Release-candidate validation and API evidence

Reads the frozen API/backend evidence and executes source-level Gold,
reference, and neutral R/Python fixture checks without implying runtime
certification.
